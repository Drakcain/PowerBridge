# Home Device Relay Shared Contracts Placeholder

This folder is a placeholder for future shared contract assets related to `Home Device Relay`.

Current status:

* docs-first only
* no runtime Kotlin models yet
* no generated schemas yet
* no transport code yet

Intended future use:

* shared schema notes
* shared Kotlin data models
* shared validation helpers
* contract test fixtures using placeholder-only data

Public-safety rules:

* do not place real tokens, secrets, credentials, or personal environment values here
* do not place generated diagnostics, live QR payloads, or real local network captures here
* keep runtime implementation out of this folder until a deliberate shared-contract extraction is approved
